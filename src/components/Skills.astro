---
import SKILLS from '@/static/skills';
import { Image } from 'astro:assets';

import '@/styles/skills.css';
---

<section class='skills section' id='skills'>
	<div class='container'>
		<h2 class='section-title px-15'>Skills</h2>
		<div class='skills-container'>
			{
				SKILLS.map((cat) => (
					<>
						<input
							type='radio'
							class='skills-tabs-radio'
							name='tabs-example'
							id={cat.categoryName}
							checked
						/>
						<label for={cat.categoryName} class='skills-tab'>
							<i class={cat.fontAwesomeClasses} />

							<div>
								<h1 class='skills-title'>{cat.categoryName}</h1>
							</div>
						</label>
						<div class='skills-group'>
							{cat.skills.map((skill) => (
								<div class='skill-container'>
									<Image
										class={skill.dark ? 'skill-image-lighten' : ''}
										src={skill.image}
										alt={skill.name}
										width={52}
										height={52}
										loading='lazy'
										crossorigin='anonymous'
									/>
									<h3 class='skill-name'>{skill.name}</h3>
								</div>
							))}
						</div>
					</>
				))
			}
		</div>
	</div>
</section>
